using System;
using Photon.Pun;

namespace Sources.BoundedContexts.Networks
{
    public class PlayerSpawnerView : MonoBehaviourPunCallbacks
    {
        private void Start()
        {
            // if (PhotonNetwork.IsConnected)
            // {
            //     PhotonNetwork.Instantiate();
            // }
        }
    }
}